# Episci distributed sidelink BLER — machine map (same semantics as legacy machines.conf)
# Format: MACHINE_ID=hostname:noise_powers_csv
# hostname may be "localhost" for the controller that runs this package.

M1=l3:-12,-10
M2=l4:-8,-6
M3=l5:-4,-2
M4=localhost:0,2,4
